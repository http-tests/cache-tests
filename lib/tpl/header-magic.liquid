{%- assign mytype = header[1] | typeof -%}
{{ header[0] }}: {% if mytype == 'number' -%}
  [ {{ header[1] | toLocaleString }} seconds from response time ]
{%- else -%}
  {{ header[1] }}
{%- endif -%}
