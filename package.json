{
  "name": "http-cache-tests",
  "version": "0.3.0",
  "description": "Tests for HTTP caches",
  "homepage": "",
  "author": "Mark Nottingham",
  "license": "BSD-3-Clause",
  "engines": {
    "node": ">=14.8.0"
  },
  "dependencies": {
    "ajv": "^6.12.5",
    "font-awesome": "^4.7.0",
    "http-server": "^0.12.1",
    "jsonschema": "^1.2.10",
    "marked": "^0.8.2",
    "node-fetch": "^2.2.0"
  },
  "scripts": {
    "server": "node server/server.mjs",
    "cli": "node --no-warnings cli.mjs",
    "export": "node --no-warnings export.mjs",
    "validate": "node --no-warnings export.mjs validate",
    "lint": "standard *.mjs client/*.mjs server/*.mjs lib/*.mjs tests/*.mjs",
    "fix": "standard --fix *.mjs client/*.mjs server/*.mjs lib/*.mjs tests/*.mjs"
  },
  "config": {
    "protocol": "http",
    "port": "8000",
    "base": "",
    "id": ""
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mnot/cdn-tests.git"
  },
  "devDependencies": {
    "standard": "^14.3.4",
    "ajv": "^6.12.5"
  }
}
