{
  "name": "http-cache-tests",
  "version": "0.3.0",
  "description": "Tests for HTTP caches",
  "homepage": "",
  "author": "Mark Nottingham",
  "license": "BSD-3-Clause",
  "engines": {
    "node": ">=14.8.0"
  },
  "dependencies": {
    "font-awesome": "^4.7.0",
    "http-server": "^0.12.1",
    "marked": "^0.8.2",
    "node-fetch": "^2.2.0"
  },
  "scripts": {
    "server": "node server/server.mjs",
    "cli": "node --experimental-modules --no-warnings cli.mjs",
    "export": "node --experimental-modules --no-warnings export.mjs",
    "lint": "standard *.js *.mjs client/*.mjs server/*.mjs tests/*.mjs",
    "fix": "standard --fix *.js *.mjs client/*.mjs server/*.mjs tests/*.mjs"
  },
  "config": {
    "protocol": "http",
    "port": "8000",
    "base": "",
    "id": ""
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mnot/cdn-tests.git"
  },
  "devDependencies": {
    "standard": "^14.3.4"
  }
}
